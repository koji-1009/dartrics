/// Build-time version string. Kept in sync with `pubspec.yaml`'s
/// `version:` field by hand — the publish checklist in `tmp/v0.1.0_plan.md`
/// notes the bump must touch both files.
const String dartricsVersion = '0.1.0';
