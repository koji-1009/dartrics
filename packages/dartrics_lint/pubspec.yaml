name: dartrics_lint
description: Analyzer plugin that surfaces dartrics's lightweight function-level metric violations as diagnostics.
version: 0.0.1
# repository: https://github.com/koji-1009/dartrics
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: ^13.0.0
  analyzer_plugin: ^0.14.0
  dartrics:
    path: ../../

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
